ticketing.content:
  path: '/open-a-ticket/{ticket_id}'
  defaults:
    _controller: '\Drupal\ticketing\Controller\TicketingController::doRedirect'
    _title: 'Ticket redirect'
    ticket_id: NULL
  requirements:
    _permission: 'access content'
  options:
    no_cache: 'TRUE'

