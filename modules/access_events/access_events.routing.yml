access_events.event_create:
  path: '/api/2.3/events'
  defaults:
    _controller: '\Drupal\access_events\Controller\EventCrudApiController::createEvent'
  methods: [POST]
  requirements:
    _user_is_logged_in: 'TRUE'
    _custom_access: 'access_affinitygroup.acting_user_access:check'
    # State-changing route: require a valid X-CSRF-Token header (from
    # /session/token). Custom routes do not get REST's automatic CSRF check,
    # so without this a plain cookie-authenticated POST is CSRF-able.
    _csrf_request_header_token: 'TRUE'
  options:
    no_cache: 'TRUE'

access_events.event_update:
  path: '/api/2.3/event-series/{eventseries}'
  defaults:
    _controller: '\Drupal\access_events\Controller\EventCrudApiController::update'
  methods: [PATCH]
  requirements:
    eventseries: '\d+'
    _user_is_logged_in: 'TRUE'
    _custom_access: 'access_affinitygroup.acting_user_access:check'
    # State-changing route: require a valid X-CSRF-Token header (from
    # /session/token). Custom routes do not get REST's automatic CSRF check,
    # so without this a plain cookie-authenticated PATCH is CSRF-able.
    _csrf_request_header_token: 'TRUE'
  options:
    parameters:
      eventseries:
        type: 'entity:eventseries'
    no_cache: 'TRUE'

access_events.event_delete:
  path: '/api/2.3/event-series/{eventseries}'
  defaults:
    _controller: '\Drupal\access_events\Controller\EventCrudApiController::delete'
  methods: [DELETE]
  requirements:
    eventseries: '\d+'
    _user_is_logged_in: 'TRUE'
    _custom_access: 'access_affinitygroup.acting_user_access:check'
    # State-changing route: require a valid X-CSRF-Token header (from
    # /session/token). Custom routes do not get REST's automatic CSRF check,
    # so without this a plain cookie-authenticated DELETE is CSRF-able.
    _csrf_request_header_token: 'TRUE'
  options:
    parameters:
      eventseries:
        type: 'entity:eventseries'
    no_cache: 'TRUE'

access_events.event_restore:
  path: '/api/2.3/event-series/{eventseries}/restore'
  defaults:
    _controller: '\Drupal\access_events\Controller\EventCrudApiController::restore'
  methods: [POST]
  requirements:
    eventseries: '\d+'
    _user_is_logged_in: 'TRUE'
    _custom_access: 'access_affinitygroup.acting_user_access:check'
    # State-changing route: require a valid X-CSRF-Token header (from
    # /session/token). Custom routes do not get REST's automatic CSRF check,
    # so without this a plain cookie-authenticated POST is CSRF-able.
    _csrf_request_header_token: 'TRUE'
  options:
    parameters:
      eventseries:
        type: 'entity:eventseries'
    no_cache: 'TRUE'

access_events.event_send_for_review:
  path: '/api/2.3/event-series/{eventseries}/send-for-review'
  defaults:
    _controller: '\Drupal\access_events\Controller\EventCrudApiController::sendForReview'
  methods: [POST]
  requirements:
    eventseries: '\d+'
    _user_is_logged_in: 'TRUE'
    _custom_access: 'access_affinitygroup.acting_user_access:check'
    # State-changing route: require a valid X-CSRF-Token header (from
    # /session/token). Custom routes do not get REST's automatic CSRF check,
    # so without this a plain cookie-authenticated POST is CSRF-able.
    _csrf_request_header_token: 'TRUE'
  options:
    parameters:
      eventseries:
        type: 'entity:eventseries'
    no_cache: 'TRUE'

access_events.event_cancel_occurrence:
  path: '/api/2.3/event-occurrences/{eventinstance}'
  defaults:
    _controller: '\Drupal\access_events\Controller\EventCrudApiController::cancelOccurrence'
  methods: [DELETE]
  requirements:
    eventinstance: '\d+'
    _user_is_logged_in: 'TRUE'
    _custom_access: 'access_affinitygroup.acting_user_access:check'
    # State-changing route: require a valid X-CSRF-Token header (from
    # /session/token). Custom routes do not get REST's automatic CSRF check,
    # so without this a plain cookie-authenticated DELETE is CSRF-able.
    _csrf_request_header_token: 'TRUE'
  options:
    parameters:
      eventinstance:
        type: 'entity:eventinstance'
    no_cache: 'TRUE'

access_events.event_restore_occurrence:
  path: '/api/2.3/event-occurrences/{eventinstance}/restore'
  defaults:
    _controller: '\Drupal\access_events\Controller\EventCrudApiController::restoreOccurrence'
  methods: [POST]
  requirements:
    eventinstance: '\d+'
    _user_is_logged_in: 'TRUE'
    _custom_access: 'access_affinitygroup.acting_user_access:check'
    # State-changing route: require a valid X-CSRF-Token header (from
    # /session/token). Custom routes do not get REST's automatic CSRF check,
    # so without this a plain cookie-authenticated POST is CSRF-able.
    _csrf_request_header_token: 'TRUE'
  options:
    parameters:
      eventinstance:
        type: 'entity:eventinstance'
    no_cache: 'TRUE'

access_events.event_edit_occurrence:
  path: '/api/2.3/event-occurrences/{eventinstance}'
  defaults:
    _controller: '\Drupal\access_events\Controller\EventCrudApiController::editOccurrence'
  methods: [PATCH]
  requirements:
    eventinstance: '\d+'
    _user_is_logged_in: 'TRUE'
    _custom_access: 'access_affinitygroup.acting_user_access:check'
    # State-changing route: require a valid X-CSRF-Token header (from
    # /session/token). Custom routes do not get REST's automatic CSRF check,
    # so without this a plain cookie-authenticated PATCH is CSRF-able.
    _csrf_request_header_token: 'TRUE'
  options:
    parameters:
      eventinstance:
        type: 'entity:eventinstance'
    no_cache: 'TRUE'

access_events.event_add_occurrence:
  path: '/api/2.3/event-series/{eventseries}/occurrence'
  defaults:
    _controller: '\Drupal\access_events\Controller\EventCrudApiController::addOccurrence'
  methods: [POST]
  requirements:
    eventseries: '\d+'
    _user_is_logged_in: 'TRUE'
    _custom_access: 'access_affinitygroup.acting_user_access:check'
    # State-changing route: require a valid X-CSRF-Token header (from
    # /session/token). Custom routes do not get REST's automatic CSRF check,
    # so without this a plain cookie-authenticated POST is CSRF-able.
    _csrf_request_header_token: 'TRUE'
  options:
    parameters:
      eventseries:
        type: 'entity:eventseries'
    no_cache: 'TRUE'

access_events.registrations_list:
  path: '/api/1.0/registrations'
  defaults:
    _controller: '\Drupal\access_events\Controller\RegistrationApiController::list'
  methods: [GET]
  requirements:
    _user_is_logged_in: 'TRUE'
    _custom_access: 'access_affinitygroup.acting_user_access:check'
  options:
    no_cache: 'TRUE'

access_events.registrations_cancel:
  path: '/api/1.0/registrations/{registrant_id}'
  defaults:
    _controller: '\Drupal\access_events\Controller\RegistrationApiController::cancel'
  methods: [DELETE]
  requirements:
    _user_is_logged_in: 'TRUE'
    _custom_access: 'access_affinitygroup.acting_user_access:check'
    # State-changing route: require a valid X-CSRF-Token header (from
    # /session/token). Custom routes do not get REST's automatic CSRF check,
    # so without this a plain cookie-authenticated DELETE is CSRF-able.
    _csrf_request_header_token: 'TRUE'
  options:
    no_cache: 'TRUE'

access_events.event_detail:
  path: '/api/2.3/events/{eventinstance}'
  defaults:
    _controller: '\Drupal\access_events\Controller\EventDetailApiController::get'
  methods: [GET]
  requirements:
    eventinstance: '\d+'
    _custom_access: 'access_affinitygroup.acting_user_access:resolve'
  options:
    parameters:
      eventinstance:
        type: 'entity:eventinstance'

access_events.event_register:
  path: '/api/1.0/events/{eventinstance}/register'
  defaults:
    _controller: '\Drupal\access_events\Controller\EventDetailApiController::register'
  methods: [POST]
  requirements:
    eventinstance: '\d+'
    _user_is_logged_in: 'TRUE'
    _custom_access: 'access_affinitygroup.acting_user_access:check'
  options:
    parameters:
      eventinstance:
        type: 'entity:eventinstance'
    no_cache: 'TRUE'

event.eventApprove:
  path: '/events/{event_id}/waitlist/approve'
  defaults:
    _controller: '\Drupal\access_events\Controller\EventWaitlist::approve'
    _title: 'Approved'
  requirements:
    _custom_access:  '\Drupal\access_events\Controller\EventWaitlist::isAuthor'
  options:
    no_cache: 'TRUE'
event.eventDeny:
  path: '/events/{event_id}/waitlist/unapprove'
  defaults:
    _controller: '\Drupal\access_events\Controller\EventWaitlist::unapprove'
    _title: 'Unapproved'
  requirements:
    _custom_access:  '\Drupal\access_events\Controller\EventWaitlist::isAuthor'
  options:
    no_cache: 'TRUE'
event.postsurvey:
  path: '/events/{entity}/post_survey/{user}'
  defaults:
    _controller: '\Drupal\access_events\Controller\EventPostSurvey::clicked'
    _title: 'Survey Clicked'
  requirements:
    _permission: 'access content'
  options:
    no_cache: 'TRUE'
